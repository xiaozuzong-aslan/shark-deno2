

type RecordItem = {
    id: string;
    data: addDate;
}
type addDate = {
    addToggle: boolean;
    currentKind?: string;
    type: string;
    notes: string;
    number: string;
    createdAt?: string;
}
