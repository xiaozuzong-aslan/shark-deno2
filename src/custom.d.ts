
type RecordItem = {
    id: string;
    data: addDate;
}
type addDate = {
    currentKind?: currentKind;
    type: string;
    notes: string;
    number: string;
    createdAt?: string;
}
type currentKind = {
    iconName?: string;
    textName?: string;
}

